name: skactiveml

on:
    pull_request:
        types: [opened, reopened, synchronize]

jobs:
  test_oldest_base_dependencies:
    if: ${{ github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [ '3.10', '3.11', '3.12', '3.13' ]
    steps:
    - uses: actions/checkout@v6
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install oldest dependencies
      run: |
        sudo apt-get -qq update
        sudo apt-get install pandoc
        sudo apt-get install ghostscript
        sudo apt-get install ffmpeg
        python -m pip install --upgrade pip
        sed 's/[>~]=/==/g' requirements.txt > requirements_old.txt
        pip install -r requirements_old.txt
        pip install -r requirements_extra.txt
    - name: Test with pytest and oldest dependencies
      run: |
        pytest

  test_newest_base_dependencies:
    if: ${{ github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [ '3.10', '3.11', '3.12', '3.13']
    steps:
    - uses: actions/checkout@v6
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install newest dependencies
      run: |
        sudo apt-get -qq update
        sudo apt-get install pandoc
        sudo apt-get install ghostscript
        sudo apt-get install ffmpeg
        python -m pip install --upgrade pip
        pip install --upgrade -r requirements.txt 
        pip install -r requirements_extra.txt
    - name: Test with pytest and newest dependencies
      run: |
        pytest

  test_oldest_optional_dependencies:
    if: ${{ github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [ '3.10', '3.11', '3.12', '3.13']
    steps:
    - uses: actions/checkout@v6
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install newest dependencies
      run: |
        sudo apt-get -qq update
        sudo apt-get install pandoc
        sudo apt-get install ghostscript
        sudo apt-get install ffmpeg
        python -m pip install --upgrade pip
        sed 's/[>~]=/==/g' requirements.txt > requirements_old.txt
        sed 's/[>~]=/==/g' requirements_opt.txt > requirements_opt_old.txt
        pip install -r requirements_old.txt
        pip install -r requirements_opt_old.txt
        pip install -r requirements_extra.txt
        pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
    - name: Test with pytest and newest dependencies
      run: |
        pytest --cov-fail-under=100

  test_newest_optional_dependencies:
    if: ${{ github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [ '3.10', '3.11', '3.12', '3.13']
    steps:
    - uses: actions/checkout@v6
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        name: cov_${{ matrix.python-version }}
        path: coverage.xml
        retention-days: 5
    - name: Install newest dependencies
      run: |
        sudo apt-get -qq update
        sudo apt-get install pandoc
        sudo apt-get install ghostscript
        sudo apt-get install ffmpeg
        python -m pip install --upgrade pip
        pip install --upgrade -r requirements.txt
        pip install --upgrade -r requirements_opt.txt
        pip install -r requirements_extra.txt
        pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
    - name: Test with pytest and newest dependencies
      run: |
        pytest --cov-fail-under=100

  check_formatting_and_linting:
    if: ${{ github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [ '3.13' ]
    steps:
    - uses: actions/checkout@v6
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install newest dependencies
      run: |
        sudo apt-get -qq update
        sudo apt-get install pandoc
        sudo apt-get install ghostscript
        sudo apt-get install ffmpeg
        python -m pip install --upgrade pip
        pip install --upgrade -r requirements.txt
        pip install --upgrade -r requirements_opt.txt -r requirements_opt_max.txt
        pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
        pip install -r requirements_extra.txt
    - name: Black
      uses: psf/black@stable
      with:
        options: "--check"
        src: "./skactiveml"
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 skactiveml/ --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 skactiveml/ --ignore=E203,W503 --count --exit-zero --statistics

  create_documentation:
    if: ${{github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [ '3.13' ]
    steps:
    - uses: actions/checkout@v6
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v6
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install newest dependencies
      run: |
        sudo apt-get -qq update
        sudo apt-get install pandoc
        sudo apt-get install ghostscript
        sudo apt-get install ffmpeg
        python -m pip install --upgrade pip
        pip install --upgrade -r requirements.txt
        pip install --upgrade -r requirements_opt.txt -r requirements_opt_max.txt
        pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
        pip install -r requirements_extra.txt
    - name: Create documentation with sphinx
      run: |
        sphinx-build -b html docs docs/_build
        zip -r docs.zip docs
    - name: 'Upload Docs Artifact'
      uses: actions/upload-artifact@v5
      with:
        name: docs_${{ matrix.python-version }}.zip
        path: docs
        retention-days: 5